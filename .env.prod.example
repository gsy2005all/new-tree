APP_ENV=prod
HOST=0.0.0.0
PORT=8000
VERSION=1.0.0
# ⚠️ 部署前务必改成一个长随机字符串！生成方法：
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET
LOG_FORMAT={time:YYYY-MM-DD at HH:mm:ss} | {level} | {message}
